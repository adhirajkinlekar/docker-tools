# ── HTTP – used during initial cert issuance and for redirect after ──────────

server {
    listen 80;
    server_name YOUR_DOMAIN;                  # replaced by init-letsencrypt.sh

    # Let's Encrypt ACME challenge (certbot writes files here)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Everything else → HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# ── HTTPS – only active after the first certificate is obtained ───────────────
# This block is initially commented out and uncommented by init-letsencrypt.sh.
# Do not edit the HTTPS_BLOCK_START / HTTPS_BLOCK_END markers.

# HTTPS_BLOCK_START
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name YOUR_DOMAIN;
#
#     # ── TLS ──────────────────────────────────────────────────────────────────
#     ssl_certificate     /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#     include             /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
#
#     # ── Security headers ─────────────────────────────────────────────────────
#     add_header Strict-Transport-Security "max-age=63072000" always;
#     add_header X-Frame-Options           DENY;
#     add_header X-Content-Type-Options    nosniff;
#
#     # ── Proxy to UI ──────────────────────────────────────────────────────────
#     location / {
#         proxy_pass         http://ui:3000;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         # ── SSE / streaming – CRITICAL ───────────────────────────────────────
#         # Disables nginx buffering so SSE tokens reach the browser immediately.
#         proxy_buffering              off;
#         proxy_cache                  off;
#         proxy_read_timeout           600s;   # long-running agent loops
#         proxy_send_timeout           600s;
#         chunked_transfer_encoding    on;
#         add_header X-Accel-Buffering no;
#
#         # WebSocket upgrade (future-proof)
#         proxy_set_header Upgrade    $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
# HTTPS_BLOCK_END
